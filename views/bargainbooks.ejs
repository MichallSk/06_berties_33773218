<!DOCTYPE html>
<html>
<head>
    <title>Bertie's Books: Bargain Books!</title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
</head>
<body>
    <h1>Bertie's Books: Bargain Books!</h1>
    
    <p>Here are all the books priced under £20.00:</p>

    <ul>
        <!-- Loop through each bargain book and display name and price -->
        <% bargainBooks.forEach(function(book) { %>
            <!-- Convert price to number with parseFloat(), then format to 2 decimal places -->
            <li><%= book.name %> - £<%= parseFloat(book.price).toFixed(2) %></li>
        <% }); %>
    </ul>

</body>
</html>